using EBills.Domain;
using EBills.IntegrationTests.Base;
using NUnit.Framework;
using System;

namespace EBills.IntegrationTests
{
    [TestFixture]
    public class LocalizationTest : TestBase
    {
        [Test]
        public void can_read_Localization()
        {
            ////Arrange
            //var localization = CreateLocalizaton();

            ////Act
            //Session.Clear();

            //var localization2 = Session.Load<Localization>(localization.Id);
            ////Assert
            //Assert.IsTrue(localization2.Id != Guid.Empty);
            //Assert.IsTrue(localization.Id == localization2.Id);
        }
    }
}
